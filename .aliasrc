alias ll='ls -lha --color=always'
alias ls='ls --color=always'
alias nv='nvim'
alias untar='tar -xzvf'

alias -s md=$BROWSER

# alias rm='move_to_trash(){ /bin/mv -f $@ ~/.trash/"$(echo "$@" | md5sum | head -c 32)"; };move_to_trash $@'
